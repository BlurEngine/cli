---
"@blurengine/cli": minor
---

Compact noisy BDS scripting logs during `blr dev` by default, with config, environment, and CLI overrides. The local-server output relay now preserves interactive terminal colors while removing extra blank lines and terminal control noise from scripting log output.
