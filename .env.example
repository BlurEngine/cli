# Optional. Only needed when bedrock-samples is not available at one of the
# conventional sibling checkout locations relative to this repo:
# - ../forks/bedrock-samples
# - ../../forks/bedrock-samples
#
# Do not commit .env.local with a real machine path.
# BLR_BEDROCK_SAMPLES_DIR=D:/path/to/bedrock-samples
